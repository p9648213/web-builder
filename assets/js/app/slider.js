export function setupHotPropertySlider() {
  // const hotPropertiesSliderEl = document.getElementById(
  //   "hot-properties-slider"
  // );
  // let properties = hotPropertiesSliderEl.childNodes;
  // let chunkSize = 6;
  // let properties_chunks = [];
  // for (let i = 0; i < properties.length; i += chunkSize) {
  //   let chunk = [];
  //   for (let j = i; j < i + chunkSize && j < properties.length; j++) {
  //     chunk.push(properties[j]);
  //   }
  //   properties_chunks.push(chunk);
  // }
  // hotPropertiesSliderEl.classList.remove("hidden");
  // hotPropertiesSliderEl.classList.add("grid");
}
